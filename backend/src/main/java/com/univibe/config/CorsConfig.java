package com.univibe.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class CorsConfig {
  // CORS ahora se maneja completamente en SecurityConfig
  // Este archivo se mantiene por compatibilidad pero ya no define beans
}
